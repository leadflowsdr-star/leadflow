# Render Free-Tier Blueprint Specification (render.yaml)
# Developed autonomously by Elena AI SDR for LeadFlow.AI
# This file enables 1-click 100% FREE deployment of your entire backend on Render's Free Web Service tier!

services:
  - type: web
    name: elena-sdr-backend
    env: python
    plan: free # Explicitly set to Render's 100% Free plan
    buildCommand: pip install -r requirements.txt
    startCommand: python web_app.py
    autoDeploy: true
